import 'package:flutter/material.dart';

class CadastroFuncionarioPage extends StatelessWidget {

  const CadastroFuncionarioPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Cadastrar funcionário'),centerTitle: true,),
           body: Container(),
       );
  }
}