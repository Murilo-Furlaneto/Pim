import 'package:flutter/material.dart';

class FolhaPagamentoPage extends StatelessWidget {

  const FolhaPagamentoPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Folha de pagamento'),centerTitle: true,),
           body: Container(),
       );
  }
}